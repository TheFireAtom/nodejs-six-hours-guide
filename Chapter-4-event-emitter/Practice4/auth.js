// Main emitter function

// Needed requirements

const fs = require("fs");
const fsPromises = require("fs").promises;
const path = require("path");
const process = require("process");
const myReadline = require("./myReadline");

// myAunth function

const authtorization = async (login, password) => {
    
}