// logEvents module

// Requirements
//(npm libraries)
const { format } = require("date-fns");
const { v4: uuid } = require("uuid");
// (nodejs libraries)
const fs = require("fs");
const path = require("path");
const fsPromises = require("fs").promises;


const logEvents = async(filePath, )